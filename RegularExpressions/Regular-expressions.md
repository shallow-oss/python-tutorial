# Regular expressions

[正则表达式HOWTO](https://docs.python.org/zh-cn/3/howto/regex.html#)

## 1.元字符
```txt
元字符
. ^ $ * + ? { } [ ] \ | ( )
```

```txt
 . 匹配除换行符外的任意字符
```

```txt
 ^ 行的开头匹配
 ^From -> 在行开头匹配From
```

```txt
 $ 行的末尾匹配
 }$ -> 在行末尾匹配}
```

```txt
 *     重复，指定前一个字符可以匹配零次或者多次
 +     重复，指定前一个字符可以匹配一次或更多次
 ?     重复，指定前一个字符可以匹配零次或一次
 {m,n} 重复，指定前一个字符可以匹配 m - n 次
       (m,n) 非必须，缺失 m 会解释为最少重复 0 次，缺失 n 则解释为最多重复无限次
       {0,} -> *
       {1,} -> +
       {0,1} -> ?
```

```txt
 [ ] 指定字符类，希望匹配的字符的一个集合
 元字符（除了 \ ）在字符类中是不起作用的
 [abc] 字符可以单独列出
 [a-c] 字符可以用字符范围来表示
 ^ 放在字符类开头，可以取反字符类，即匹配除给出的字符类之外的任何字符
```

```txt
 \ 反斜杠后面可以跟各种字符来表示各种特殊的序列，也用于转义元字符
 \[ -> [
 \\ -> \
 特殊序列：
     \d 匹配任何十进制数字 -> [0-9]
     \D 匹配任何非数字字符 -> [^0-9]
     \s 匹配任何空白字符 -> [ \t\n\r\f\v]
     \S 匹配任何非空白字符 -> [^ \t\n\r\f\v]
     \w 匹配任何字母与数字字符 -> [a-zA-Z0-9_]
     \W 匹配任何非字母与数字字符 -> [^a-zA-Z0-9_]
     \A 仅匹配字符串开头，不在 MULTILINE 模式时，\A 和 ^ 实际上是相同的
     \Z 仅匹配字符串尾
     \b 字边界，仅在单词的开头或结尾处匹配
     \B 当前位置不在字边界时才匹配，与 \b 相反
 特殊序列可以被包含在字符类里面。ex: [\s,.] 匹配任何空白字符、',' 或 '.' 的字符类
```

```txt
 | A 和 B 是正则表达式，A|B 将匹配任何与 A 或 B 匹配的字符串
```

```txt
 ( ) 表示分组，可以配合 group() 等方法使用
```

## 2.方法 / 属性
```txt
 re 方法 / 属性
   match()    检查一个字符串是否以指定的模式开始，未找到返回 None，找到返回匹配对象
   search()   扫描字符串，查找此正则匹配的任何位置，未找到返回 None，找到返回匹配对象
   findall()  找到正则匹配的所有子字符串，未找到返回空列表，找到返回匹配到的字符串列表，
   finditer() 找到正则匹配的所有子字符串，未找到返回空列表，返回包含匹配对象的列表

 匹配对象：起始和终结位置、匹配的子串以及其它
 匹配对象 方法 / 属性
   group()     返回正则匹配的字符串
   start()     返回匹配的开始位置
   end()       返回匹配的结束位置
   span()      返回包含匹配 (start, end) 位置的元组
```